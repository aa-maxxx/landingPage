
  # Household Services Landing Page

  This is a code bundle for Household Services Landing Page. 

  ## Running the code

  Run `pnpm i` to install the dependencies.

  Run `pnpm dev` to start the development server.
  